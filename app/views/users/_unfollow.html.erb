<%= form_for(current_user.active_relationships.select { |ar| ar.followed_id == locals[:user].id }, html: { method: :delete, remote: true }) do |f| %>
  <div><%= hidden_field_tag :followed_id, locals[:user].id %></div>
  <%= f.submit 'Following', class: 'relationship_btn unfollow' %>
<% end %>
