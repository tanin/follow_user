$('<%= "#followers_count_#{@followed_user.id}" %>').html("<%= @followed_user.relationships_count %>");
$('<%= "#follow_form_#{@followed_user.id}" %>').html("<%= escape_javascript(render('users/follow', locals: { user: @followed_user })) %>");
